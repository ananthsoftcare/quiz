import { Card, Button, Typography, Box, Stepper, Step, StepLabel, StepContent, Paper } from '@mui/material';
import { useState } from 'react';

export default function stepperComp(){
    
      return (
        <div><h1>Stepper</h1></div>     
      );

}